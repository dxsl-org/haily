{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Core capabilities for Haily Mobile, valid on every build target (including this host's own `cargo check`, which never compiles the Android/iOS-only plugin crates at all).",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
