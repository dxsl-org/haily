"""Planted defect (the task): merge_intervals is unimplemented. Implement it so the tests in
test_intervals.py pass. Do NOT change the tests."""


def merge_intervals(intervals):
    """Merge overlapping/adjacent (start, end) intervals; return them sorted, non-overlapping."""
    raise NotImplementedError
