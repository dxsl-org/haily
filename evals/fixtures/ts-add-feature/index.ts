// Planted defect (the task): slugify is unimplemented and throws. Implement it so `npm test`
// passes against the contract in index.test.ts. Do NOT change the test.

export function slugify(input: string): string {
  throw new Error("not implemented");
}
