# Rust / Cargo
/target/
**/*.rs.bk

# Node / SvelteKit
node_modules/
build/
.svelte-kit/
.env
.env.*
!.env.example

# Tauri build artifacts
src-tauri/target/
src-tauri/WixTools/

# Haily runtime data — never commit these
*.gguf
*.db
*.db-shm
*.db-wal
haily.log
haily-*.log

# Agent plans — gitignored per docs/documentation-management
.agents/
.docs/
models/
data/

# Local dev artifacts
haily.exe
.logs/
src-tauri/.logs/

# Reference codebase (old Go version — local only)
.reference/

# Claude Code personal config
.claude/

# Downloaded model caches (fastembed ONNX weights — pulled at runtime, never committed)
.fastembed_cache/
**/.fastembed_cache/

# OS
.DS_Store
Thumbs.db
